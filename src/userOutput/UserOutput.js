import React from "react";

const userOutput = (props) => {
  return (
    <div className="UserOutput">
    <p> This is {props.name} project </p>
    <p> Lets give it a try </p>
    </div>
  )
}

export default userOutput;
